# 自由ポーズ画面・右手先端 Hotfix

## 修正内容

- 自由ポーズ編集ボタンの遷移を、カスタムイベント依存から実URL (`?tab=pose`) への遷移へ変更。
- 編集データは sessionStorage を優先し、利用できない場合は localStorage へフォールバック。
- 専用画面からロボット工房へ戻る場合も実URL (`?tab=robot`) で確実に遷移。
- 腕先の皿ねじ方向を左右鏡対称に変更。左手は腕軸 +90°、右手は -90°。
- 側面・背面の頭部形状は今回変更していない。次回、ユーザー提供のPowerPoint図を基準に再設計する。

## DB

追加SQLなし。保存形式の変更なし。
