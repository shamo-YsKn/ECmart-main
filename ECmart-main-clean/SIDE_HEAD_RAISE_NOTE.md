# Side head raise note

今回の修正では、側面表示の下段頭部(六角部分)を上へ持ち上げました。

変更内容:
- 下段頭部本体: y=96..140 → y=64..108
- 上方向への移動量: 32px
- 目ねじクリップ領域: y=96..140 → y=64..108
- 目ねじ本体も同量だけ上へ追従

これにより、画像の赤点線(旧 y=96)から赤実線(上段下端 y=64)までの距離だけ、
下段頭部を上へ移動しています。
